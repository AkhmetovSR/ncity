// app/page.tsx
import React from "react";
import Main from "@/components/Main/Main"; // Импортируем наше интерактивное клиентское ядро интерфейса

/**
 * 🌟 ГЛАВНАЯ СТРАНИЦА ПОРТАЛА (Server Component / SSR)
 * Этот компонент выполняется строго на сервере Next.js [INDEX].
 * Роботы Яндекса и Google увидят чистый, готовый к индексации HTML-код главной страницы.
 */
export default function HomePage() {
    return (
        /*
           🌟 ДЕКЛАРАТИВНЫЙ СТАРТ SPA-КОНТУРА:
           Поскольку пользователь зашел на корень сайта ("/", главная страница),
           мы декларативно прописываем начальные параметры (initial-пропсы) как null [INDEX].

           Когда клиентский React проснется (гидратируется), компонент Main сразу поймет:
           "Мы находимся на чистой главной странице, открывать модалки при первой загрузке не нужно" [INDEX].
           А хукuseAppHistory просто встанет на дежурство и начнет слушать адресную строку [INDEX].
        */
        <Main
            initialActiveId={null}
            initialVacancyId={null}
        />
    );
}


// Команда для локального PWA-тестирования по HTTPS на мобильном устройстве:
// "dev": "next dev --experimental-https --hostname 192.168.0.100"
